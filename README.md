To run this, you need to manually download the jar file for sqlite connector. <br>
After downloading it, navigate to the properties inside your specific IDE, then add the path or you can simply drag the jar file you just downloaded. <br>
This should fix the missing database connector.

